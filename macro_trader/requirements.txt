# Macro trader module dependencies
# These should already be in your bot's main requirements.txt
# Listed here for reference

# AWS SDK
boto3>=1.26.0
botocore>=1.29.0

# Date/time parsing (for market resolution dates)
python-dateutil>=2.8.2

# Type hints
typing-extensions>=4.0.0 ; python_version < '3.10'

# Async support (built-in to Python 3.7+, listed for clarity)
# No additional package needed if using asyncio

# Optional: For local testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
moto>=4.0.0  # AWS mocking for tests
