# HTTP client
requests==2.32.3

# AWS SDK
boto3==1.35.99
botocore==1.35.99

# Scheduling (local runs only — not needed in Lambda)
APScheduler==3.10.4

# Environment variables
python-dotenv==1.0.1

# Anthropic Claude API
anthropic==0.40.0

# RSA signing for Kalshi API auth (carpet_bagger)
cryptography>=42.0.0

# Coinbase Advanced API auth (funding_rate module — JWT ES256)
PyJWT>=2.8.0

# Async HTTP client (funding_rate module)
aiohttp>=3.9.0
